# 快捷键速查

> - **Leader 键 = 空格**，LocalLeader = `,`
> - 编辑器内可随时用 `<Space>ch` 打开 NvChad 速查表，或 `:Telescope keymaps` 搜索全部按键
> - 本文档与 `lua/mappings.lua` / 插件配置保持同步，改键后请一并更新

---

## ⚠️ 与原生 Vim 不同的键

这些键的原生行为被本配置覆盖，先看这张表能避免大部分困惑：

| 按键 | 现在的功能 | 原生功能（已失效） |
| --- | --- | --- |
| `s` | 分屏前缀键（见下文） | 删除字符并进入插入模式 |
| `f` / `F` | leap 跳转（向后 / 向前搜两字符跳转） | 行内查找单字符 |
| `;` | 进入命令行（替代 `:`） | 重复上次 f/t 查找 |
| `,` | LocalLeader | 反向重复 f/t 查找 |
| `K` | LSP 悬浮文档（lspsaga） | 查 man 手册 |
| `ca` | LSP Code Action | `ca(` `caw` 等"修改包含"文本对象前缀，**快速连按 `caw` 会触发 Code Action**，改单词请用 `ciw` |
| `<C-w>` | 关闭当前 buffer | 窗口操作前缀（`<C-w>=` 均分、`<C-w>o` 等不可用，分屏操作见 `s` 前缀） |
| `<C-h>` / `<C-l>` | 上一个 / 下一个 buffer | 无（NvChad 原本用于切窗口，切窗口改用 `<A-hjkl>`） |
| `<C-j>` / `<C-k>` | 向下 / 向上移动 5 行（n、v 模式） | 无 |
| `p` / `P` | yanky 粘贴（支持历史环切换） | 行为一致，额外多了历史功能 |
| `J` / `K`（可视模式） | 上下移动选中代码块 | J 合并行（普通模式 J 仍是原生合并行） |
| `zR` / `zM` | **关闭**全部折叠 / **打开**全部折叠（ufo） | 语义与原生相反（原生 zR=打开、zM=关闭），沿用已有习惯 |
| `<C-n>` / `<C-p>` | 粘贴后切换 yanky 历史 | 无（NvChad 原本 `<C-n>` 开文件树，改用 `<A-m>`） |
| `<leader>n` | 回到 Nvdash 首页 | —（NvChad 默认的行号开关失效，需要时用 `:set nu!`） |
| `<leader>rn` | 当前文件搜索替换 | —（NvChad 默认的相对行号开关失效，需要时用 `:set rnu!`） |

---

## 基础 Vim 快捷键（精选）

### 模式切换

| 按键 | 功能 |
| --- | --- |
| `i` / `a` | 在光标前 / 后进入插入模式 |
| `I` / `A` | 在行首 / 行尾进入插入模式 |
| `o` / `O` | 在下方 / 上方新建一行并插入 |
| `v` / `V` / `<C-v>` | 字符 / 行 / 块可视模式 |
| `Esc` | 回到普通模式（本配置额外清除搜索高亮） |
| `jk` | 插入模式下快速回到普通模式（better-escape） |

### 移动

| 按键 | 功能 |
| --- | --- |
| `h j k l` | 左 下 上 右 |
| `w` / `b` / `e` | 下个词首 / 上个词首 / 词尾 |
| `0` / `^` / `$` | 行首 / 第一个非空字符 / 行尾 |
| `gg` / `G` / `{n}G` | 文件开头 / 末尾 / 第 n 行 |
| `{` / `}` | 上一段 / 下一段 |
| `%` | 匹配括号间跳转 |
| `<C-d>` / `<C-u>` | 下翻 / 上翻半屏 |
| `<C-o>` / `<C-i>` | 跳转历史后退 / 前进 |
| `zz` / `zt` / `zb` | 当前行居中 / 置顶 / 置底 |
| `f{c}{c}` | leap：输入两个字符直接跳到目标位置（`F` 反向） |

### 编辑

| 按键 | 功能 |
| --- | --- |
| `x` | 删除光标字符 |
| `dd` / `yy` / `cc` | 删除 / 复制 / 修改整行 |
| `D` / `C` / `Y` | 删除 / 修改 / 复制到行尾 |
| `r{c}` | 替换单个字符 |
| `J` | 合并下一行（普通模式） |
| `u` / `<C-r>` | 撤销 / 重做 |
| `.` | 重复上次修改 |
| `>>` / `<<` | 右 / 左缩进（可视模式 `>` `<`） |
| `ga` | 查看光标字符编码 |

### 操作符 + 文本对象（组合使用）

操作符：`d` 删除、`c` 修改、`y` 复制、`gu`/`gU` 转小写/大写。

| 文本对象 | 范围 |
| --- | --- |
| `iw` / `aw` | 单词（不含 / 含空格） |
| `i"` `i'` `` i` `` | 引号内 |
| `i(` `i[` `i{` | 括号内（`a` 系列含括号本身） |
| `it` / `at` | HTML/JSX 标签内 / 含标签 |
| `ip` / `ap` | 段落 |

例：`ciw` 改单词、`di(` 删括号内、`yi"` 复制引号内、`cit` 改标签内容。
**注意**：`ca` 开头的组合（如 `caw`）会被 Code Action 拦截，请用 `ci` 系列或等 1 秒超时。

### 搜索 / 宏 / 标记

| 按键 | 功能 |
| --- | --- |
| `/xx` / `?xx` | 向下 / 向上搜索，`n`/`N` 下一个/上一个 |
| `*` / `#` | 搜索光标下的词（向下 / 向上） |
| `m{a-z}` / `` `{a-z} `` | 打标记 / 跳到标记 |
| `q{a-z}…q` / `@{a-z}` | 录制宏 / 回放宏，`@@` 重复上次 |
| `gv` | 重新选中上次的可视区域 |

---

## 窗口与分屏

| 按键 | 功能 |
| --- | --- |
| `sv` | 垂直分屏（左右） |
| `sh` | 水平分屏（上下） |
| `sc` | 关闭当前窗口 |
| `s,` / `s.` | 窗口宽度减少 / 增加 |
| `<A-h>` `<A-j>` `<A-k>` `<A-l>` | 切换到左 / 下 / 上 / 右窗口 |

## 保存与退出

| 按键 | 功能 |
| --- | --- |
| `<leader>w` | 保存当前文件 |
| `<leader>W` | 保存所有文件 |
| `<C-s>` | 保存（NvChad 默认） |
| `<leader>q` | **不保存**强制退出全部（`qa!`，慎用） |

## Buffer / 标签栏

| 按键 | 功能 |
| --- | --- |
| `<C-h>` / `<C-l>` | 上一个 / 下一个 buffer |
| `<Tab>` / `<S-Tab>` | 下一个 / 上一个 buffer（NvChad 默认） |
| `<C-w>` / `<leader>x` | 关闭当前 buffer |
| `<leader>b` | 新建空 buffer |

## 文件树（nvim-tree）

| 按键 | 功能 |
| --- | --- |
| `<A-m>` | 开关文件树 |
| `<leader>e` | 聚焦文件树 |

树内常用（更多按 `?` 查看）：`回车` 打开、`a` 新建、`r` 重命名、`d` 删除、`x`/`c`/`p` 剪切/复制/粘贴、`y` 复制文件名、`<C-k>` 上移 5 行、`?` 帮助。

## 模糊搜索（Telescope）

| 按键 | 功能 |
| --- | --- |
| `<leader>ff` | 找文件 |
| `<leader>fa` | 找全部文件（含隐藏/忽略） |
| `<leader>fw` | 全局搜文本（live grep） |
| `<leader>fz` | 当前文件内搜索 |
| `<leader>fb` | 已打开的 buffer |
| `<leader>fo` | 最近打开的文件 |
| `<leader>fp` | 最近项目 |
| `<leader>fh` | 帮助文档 |
| `<leader>ma` | 标记列表 |
| `<leader>cm` / `<leader>gt` | git 提交记录 / git 状态 |
| `<leader>pt` | 隐藏的终端列表 |
| `<leader>th` | 切换主题 |

弹窗内：`<C-j>`/`<C-k>` 上下选择，`<C-u>`/`<C-d>` 滚动预览，`↑`/`↓` 输入历史，`Esc` 直接关闭。

## LSP

| 按键 | 功能 |
| --- | --- |
| `gd` / `gD` | 跳转定义 / 声明 |
| `<leader>D` | 跳转类型定义 |
| `gp` | lspsaga finder（引用/定义聚合面板） |
| `K` | 悬浮文档 |
| `ca` | Code Action |
| `[d` / `]d` | 上一个 / 下一个诊断 |
| `<leader>ra` | LSP 重命名符号 |
| `<leader>ds` | 诊断列表（loclist） |
| `<leader>fm` | 手动格式化（保存时也会自动格式化） |
| `grr` / `gri` / `gO` | 引用 / 实现 / 文档符号大纲（Neovim 0.11 内置） |

lspsaga 浮窗内：`<C-j>`/`<C-k>` 滚动预览，诊断窗中 `o` 执行修复、`q`/`Esc` 退出。

## 补全（blink.cmp）与代码片段

插入模式、补全菜单出现时：

| 按键 | 功能 |
| --- | --- |
| `<CR>` | 确认所选项 |
| `<Tab>` / `<S-Tab>` | 下一项 / 上一项（无菜单时跳转 snippet 占位符） |
| `<C-j>` / `<C-k>` | 下一项 / 上一项 |
| `<C-space>` | 手动打开补全菜单 / 文档 |
| `<C-e>` | 关闭菜单 |
| `<C-b>` / `<C-f>` | 文档窗口上 / 下滚动 |

插入模式光标移动（NvChad）：`<C-b>` 行首、`<C-e>` 行尾、`<C-h>`/`<C-l>` 左右。

## AI 补全（windsurf/codeium）

插入模式，虚拟文本建议出现时：

| 按键 | 功能 |
| --- | --- |
| `<A-l>` | 接受建议 |
| `<C-;>` / `<C-,>` | 下一条 / 上一条建议 |
| `<C-x>` | 清除建议 |

## 注释

| 按键 | 功能 |
| --- | --- |
| `<leader>/` | 切换注释（普通/可视模式） |
| `gcc` | 注释当前行 |
| `gc{motion}` | 注释范围，如 `gcip` 注释整段；可视模式选中后 `gc` |
| `gb{motion}` | 块注释（`/* */`） |

JSX/Vue 中会根据光标位置自动用正确的注释风格（ts-context-commentstring）。

## 折叠（ufo）

| 按键 | 功能 |
| --- | --- |
| `za` / `zo` / `zc` | 切换 / 打开 / 关闭当前折叠（原生） |
| `zR` | 关闭全部折叠（注意：与原生语义相反） |
| `zM` | 打开全部折叠（注意：与原生语义相反） |

## 复制粘贴（yanky）

| 按键 | 功能 |
| --- | --- |
| `p` / `P` | 粘贴到光标后 / 前 |
| `<C-p>` / `<C-n>` | 粘贴后立即按：切换到上一条 / 下一条复制历史 |
| `<leader>p` | Telescope 打开复制历史列表 |

## 查找替换

| 按键 | 功能 |
| --- | --- |
| `<leader>rn` | 当前文件搜索替换（rip-substitute，普通/可视模式） |
| `<leader>rl` | 全局搜索替换（grug-far） |

rip-substitute 弹窗内：`<CR>` 确认、`Esc` 取消、`↑`/`↓` 历史、`R` 在 regex101 打开当前正则。

## Git

| 按键 | 功能 |
| --- | --- |
| `<leader>gg` | 打开 LazyGit |
| `<leader>bt` | 开关行内提交信息（git blame，gitsigns） |
| `<leader>cm` | Telescope 浏览提交历史 |
| `<leader>gt` | Telescope 查看 git 状态 |

## 终端

| 按键 | 功能 |
| --- | --- |
| `<leader>h` / `<leader>v` | 新开水平 / 垂直终端 |
| `<A-i>` | 开关浮动终端 |
| `<A-v>` | 开关垂直终端 |
| `<C-x>` | 终端内：退出终端模式（回到普通模式） |
| `<A-h>` | 仅终端模式内：隐藏水平终端（普通模式的 `<A-h>` 已用于切窗口） |

## 其他

| 按键 | 功能 |
| --- | --- |
| `Esc` | 清除搜索高亮 |
| `<C-c>` | 复制整个文件内容 |
| `<leader>n` | 回到 Nvdash 首页 |
| `<leader>td` | TODO/FIXME 注释列表 |
| `<leader>ch` | NvChad 快捷键速查表 |
| `<leader>th` | 主题选择器 |
| `<C-t>` / 右键 | 打开上下文菜单 |
| `<leader>bt` | git blame 开关 |

## Nvdash 首页

| 按键 | 功能 |
| --- | --- |
| `u` | 更新插件（Lazy sync） |
| `f` | 找文件 |
| `p` | 最近项目 |
| `c` | 打开配置 |
| `t` | 切换主题 |
| `a`–`e` 等字母 | 打开对应的最近文件 / 最近目录（首页上有标注） |
